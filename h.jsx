import {useSelector} from "react-redox"
import logo from "../h"
function A(){
const value= (cart)=>{cart.value.item}
return (
    <>
    <img data-test-id="demmy-img" src={logo} alt="km"/>
    
    </>
)
export default A;
}